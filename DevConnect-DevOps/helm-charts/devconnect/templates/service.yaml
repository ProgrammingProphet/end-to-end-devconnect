apiVersion: v1
kind: Service
metadata:
  name: {{ include "devconnect.fullname" . }}
  labels:
    {{- include "devconnect.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devconnect.selectorLabels" . | nindent 4 }}
