#deploy to aks using git action cd
#Deploy to Kubernetes
name: Build DevConnect images and Upload to Docker Hub

on:
  push:
    branches:
      - main

jobs:
  # -------------------------------
  # CI: Build & Push Docker Image
  # -------------------------------
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push frontend
        uses: docker/build-push-action@v4
        with:
          context: ./frontend
          push: true
          tags: | #why this used - for multiple tags
            ${{ secrets.DOCKER_USERNAME }}/devconnect-frontend:latest
            ${{ secrets.DOCKER_USERNAME }}/devconnect-frontend:${{ github.sha }}

      - name: Build and push backend
        uses: docker/build-push-action@v4
        with:
          context: ./backend
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/devconnect-backend:latest
            ${{ secrets.DOCKER_USERNAME }}/devconnect-backend:${{ github.sha }}

  # -------------------------------
  # CD: Deploy Application
  # -------------------------------
  # deploy:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Deploy to Kubernetes
  #       uses: azure/k8s-deploy@v4
  #       with:
  #         action: deploy
  #         manifests: |
  #           k8s/namespace.yml
  #           k8s/secrets.yml
  #           k8s/configMap.yml
  #           k8s/database/persistantVolume.yml
  #           k8s/database/persistantVolumeClaim.yml
  #           k8s/database/deploymet.yml
  #           k8s/database/service.yml
  #           k8s/backend/deployment.yml
  #           k8s/backend/service.yml
  #           k8s/frontend/deployment.yml
  #           k8s/frontend/service.yml
  #           k8s/ingress.yml
  #         images: |
  #           ${{ secrets.DOCKER_USERNAME }}/devconnect-frontend:latest
  #           ${{ secrets.DOCKER_USERNAME }}/devconnect-backend:latest


# suppose i deploy to aks using git action cd
# then how can i access the application?
# answer: using the ingress controller
# ingress controller is nothing but a service of type load balancer
# steps: to access the application
# 1. get the external ip of the ingress controller
# 2. use the external ip to access the application





# locally frontend-devconnect build and push to docker hub
# docker build -t programmingprobhet/devconnect-frontend:latest frontend   # -t is for tag frontend folder for context - fast way to build and push
# docker push programmingprobhet/devconnect-frontend:latest

# locally backend-devconnect build and push to docker hub
# docker build -t programmingprobhet/devconnect-backend:latest backend   # -t is for tag backend folder for context - fast way to build and push
# docker push programmingprobhet/devconnect-backend:latest

# locally mongodb build and push to docker hub
# docker build -t programmingprobhet/mongodb:latest .
# docker push programmingprobhet/mongodb:latest

# successfully build and push to docker hub
# docker build -t programmingprobhet/devconnect-frontend:latest frontend
# docker push programmingprobhet/devconnect-frontend:latest
# docker build -t programmingprobhet/devconnect-backend:latest backend
# docker push programmingprobhet/devconnect-backend:latest